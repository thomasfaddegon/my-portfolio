---
const { centered, header, maxWidth, classes, hideBracketOnMobile } =
  Astro.props as {
    header: string;
    centered?: boolean;
    maxWidth?: string;
    classes?: string;
    hideBracketOnMobile?: boolean;
  };

const center = centered === true ? "text-center" : "";
const hideBracket = hideBracketOnMobile ? "hidden xl:flex" : "";
---

<!-- <div class="flex flex-row">
  <div class="text-purple text-2xl mt-[-2px] flex flex-col justify-center">
    <div>&lt;</div>
  </div>
  <div class={`max-w-[${maxWidth}rem] px-2 `}>
    <h3 class={`${center} ${classes} text-lg `}>{header}</h3>
  </div>
  <div class="text-purple text-2xl mt-[-2px] flex flex-col justify-center">
    <div>&gt;</div>
  </div>
</div> -->


<div class="flex gap-2">
  <h3 class={`text-xl overflow-hidden text-lightBlue`}>
    {header}
  </h3>
</div>

<style>
  h3 {
    z-index: 10;
  }
</style>
